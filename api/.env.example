# Required: password to protect all API endpoints
APP_PASSWORD=change-me

# Optional: port the API listens on (default: 3000)
PORT=3000

# Optional: directory where the SQLite database file is stored (default: ./data)
DATA_DIR=/data
