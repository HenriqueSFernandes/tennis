{
  "name": "rio-tinto",
  "private": true,
  "workspaces": [
    "api",
    "frontend"
  ],
  "overrides": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "scripts": {
    "dev:api": "npm run dev --workspace=api",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=api && npm run build --workspace=frontend",
    "format": "npm run format --workspace=api && npm run format --workspace=frontend",
    "format:check": "npm run format:check --workspace=api && npm run format:check --workspace=frontend"
  }
}
